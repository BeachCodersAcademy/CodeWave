$(function() {
  
  
  $('#chat-btn').click(function() {

    let $chat = $('textarea');        
    let chat = $chat.val();        
    $chat.val('');
    
    $chat.focus();

    // add chat to Firebase
    
    
  });
  
  
  
});